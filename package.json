{
  "name": "checkout-ap-onboarding-ui",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://daniel-munch-cko.github.io/checkout-ap-onboarding-ui/",
  "dependencies": {
    "axios": "^0.18.0",
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "react-jsonschema-form": "^1.0.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "cp -r build/* dist/ && cd dist && git add --all && git commit -m 'Deploy' && git push origin gh-pages"
  },
  "devDependencies": {}
}
